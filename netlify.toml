[build]
  command = "corepack enable && corepack prepare pnpm@9 --activate && pnpm install && pnpm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
